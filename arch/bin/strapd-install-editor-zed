#!/bin/bash

# strapd:summary=Install Zed Editor and configure it with the current strapd theme

echo "Installing Zed Editor..."
strapd-pkg-add zed omazed

omazed setup

setsid uwsm-app -- gtk-launch dev.zed.Zed >/dev/null 2>&1 &
