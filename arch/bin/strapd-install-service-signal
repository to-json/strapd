#!/bin/bash

# strapd:summary=Install Signal and launch it.
# strapd:requires-sudo=true

set -e

echo "Installing Signal..."
strapd-pkg-add signal-desktop

echo "Opening Signal..."
setsid uwsm-app -- /usr/bin/signal-desktop >/dev/null 2>&1 &

echo ""
echo "Signal has been installed."
