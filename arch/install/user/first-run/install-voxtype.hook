#!/bin/bash

set -e

if strapd-done ensure voxtype-install-invitation; then
  strapd-notification-send -u critical -g  "Install Dictation with Voxtype" \
    "Click to install voice dictation for strapd." \
    --exec strapd-launch-floating-terminal-with-presentation strapd-voxtype-install
fi
