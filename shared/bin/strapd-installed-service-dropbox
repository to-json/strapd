#!/bin/bash

# strapd:summary=Check whether Dropbox is installed and running
# strapd:hidden=true

set -euo pipefail

if strapd-cmd-present dropbox-cli && dropbox-cli running >/dev/null 2>&1; then
  exit 0
fi

pgrep -x dropbox >/dev/null 2>&1
