#!/bin/bash

# strapd:summary=Launch a terminal in the active terminal's current directory
# strapd:args=[command...]

exec setsid uwsm-app -- xdg-terminal-exec --dir="$(strapd-cmd-terminal-cwd)" "$@"
