#!/bin/bash

# strapd:summary=List available monospace fonts
# strapd:examples=strapd font list | strapd font set "CaskaydiaMono Nerd Font"

fc-list :spacing=100 -f "%{family[0]}\n" | grep -v -i -E 'emoji|signwriting|strapd' | sort -u
