#!/bin/bash

# strapd:summary=Refresh the current theme from its templates.

THEME_NAME_PATH="$HOME/.local/state/strapd/current/theme.name"

if [[ -f $THEME_NAME_PATH ]]; then
  STRAPD_THEME_SKIP_BACKGROUND=1 strapd-theme-set "$(cat $THEME_NAME_PATH)"
fi
