#!/bin/bash

# strapd:summary=Update user-installed git themes

mapfile -t themes < <(strapd-theme-extras)

for theme in "${themes[@]}"; do
  echo "Updating: $(basename "$theme")"
  git -C "$theme" pull
done
