# Your monitors. Mango picks a sensible mode and arrangement on its own, so
# this file is empty until you disagree with it.
#
# `wlr-randr` prints the names and available modes of what is plugged in right
# now.
#
# monitorrule=eDP-1,0.55,1,tile,0,1.5,0,0,1920,1080,60.00
# monitorrule=DP-1,0.55,1,tile,0,2,2560,0,3840,2160,59.99
#
# The fields are: name, mfact, nmaster, layout, transform, scale, x, y, width,
# height, refresh.
